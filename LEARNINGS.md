# 239-meeting-shift LEARNINGS

## 2026-09-16 コントロールパネル実装＋タイトル付与

- フォルダ名を `239-day063` から `239-meeting-shift` へ変更。238以前と同じ `NNN-名前` の慣例に戻した。
- **明示ポーズが無かった**。20秒の制限時間があるのに中断できず、電車プレイ前提の題材として成立していなかった。`<dialog>` + P/Esc + `visibilitychange` で3経路から止められるようにした。
- ポーズは「時間・カメラ・入力だけを止め、描画ループは回し続ける」形にした。描画ごと止めると復帰時に黒画面になる。
- **ミュートが無かった**。`tone()` の入口1箇所で遮断するだけで済んだ。音源が関数1本に集約されている設計が効いた。
- 仮想パッドが42pxで、Appleの最小タップ44ptを下回っていた。48pxへ拡大し `touch-action:none` と押下表現を追加。
- **開始幕が canvas の後ろにあった**（238で得た学びが未適用）。幕を canvas より前に移動して解決。harness のタップ判定は `button, canvas` のDOM先頭を叩くため、この順序を守らないと必ず timeout する。
- `localStorage` は `tg.239.muted` / `tg.239.best` で接頭辞を付けた。GitHub Pages は同一オリジンなので接頭辞なしキーは他ゲームと衝突する。
- 検証: harness PASS ／ 操作盤の機能テスト **20/20 PASS**（ポーズで残り時間が止まる、再開で進む、リロード後もミュート保持、5問完走でベスト保存、JSエラー0件）。
- 未検証: iPhone 実機の発熱・FPS、音声認識の実機精度。
