# 239-meeting-shift ｜ MEETING//SHIFT 3D

## 0. ドキュメント情報

- 仕様書名: MEETING//SHIFT 3D
- 対象ゲーム: `239-meeting-shift`（旧 `239-day063`）
- 作成日: 2026-09-16
- 更新日: 2026-09-16
- ステータス: 実装済み（harness PASS。iPhone 実機は未検証）
- 参照ファイル: `index.html`（単一ファイル。外部アセットなし）

## 批評（コントロールパネル実装前）

- 題材は強い。会議の「発表する側」と「答える側」を切り替えると同じ質問がまったく別物になる、という構造がゲーム性そのものになっている。
- 一方で**プレイヤーが自分で止められなかった**。20秒の制限時間つきなのに、電車で顔を上げた瞬間に時間切れになる。明示ポーズは必須。
- 効果音は鳴るが消せなかった。会議中や移動中に開く題材なのに、音を切る手段が無いのは致命的。
- 仮想パッドが42pxで、Appleの最小タップ44ptを下回っていた。
- 起動時の幕（`#startModal`）が canvas の後ろにあり、DOM先頭の操作対象が「押せない canvas」になっていた。

## 1. ゲーム概要

- ジャンル: 3D 会議シミュレータ／発言トレーニング
- 一言説明: 発表者と質問される側を切り替えて、5つの質問に20秒以内で答える。
- 想定プレイ時間: 3〜4分／1周（5問）
- 想定プレイヤー: iPhone 片手。法人営業・社内会議の前の素振り
- クリア体験の要点: 「結論→根拠→依頼」で答えると信頼が上がり、緊張が下がる手触り

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 実装方式: 単一 HTML。ビルド不要、外部CDNなし
- 描画: Canvas 2D による自前の3D投影（WebGL不使用）

### 任意
- PC: ドラッグで見回し、WASD移動、V で視点切替、Space で発言
- 音声入力: Web Speech API 対応ブラウザのみ。非対応でも最後まで遊べる

## 3. ファイル構成

```text
239-meeting-shift/
  index.html    # 全部入り（HTML/CSS/JS）
  SPEC.md
  LEARNINGS.md
```

## 4. 操作／設定UI（コントロールパネル）

**実装順の鉄則どおり、ゲームロジックより先にこの節を確定させた。**

| 項目 | 実装 |
|---|---|
| 入力方式（キー） | ドラッグ=見回す / WASD=移動 / V=視点切替 / Space=発言 / **P・Esc=ポーズ** |
| 入力方式（タッチ） | canvas ドラッグ＝見回す、右下の十字キー（**48px**）＝旋回・前後 |
| Pointer Events | canvas と十字キーの両方で `setPointerCapture`。`pointercancel` / `lostpointercapture` で確実に解除 |
| ポーズ | 右上 `Ⅱ` ボタン、または P / Esc。`<dialog>` で「再開／音の切替／タイトルへ」 |
| 自動停止 | `visibilitychange` でタブを離れたら自動ポーズ |
| ミュート | 右上 `♪` ボタン（ポーズ画面からも切替可）。`tone()` の入口で遮断 |
| 設定の保存 | `tg.239.muted`（ミュート） / `tg.239.best`（自己ベストスコア） |
| 重なり順 | 開始幕を canvas より前に置く。DOM先頭の操作対象を実際に押せるボタンにする |

- ポーズ中に止まるもの: 残り時間、カメラ移動、ドラッグ見回し、仮想パッド入力
- ポーズ中も続くもの: 背景の描画ループ（復帰時に黒画面にしないため）

## 5. ゲームルール

- 1セッション = 5問。各問 20秒
- 回答方法は2通り: 3択の「話の型」を選ぶ／自由入力（キーワード一致で採点）
- スコア = 回答品質(0〜3)×8 ＋ 速度ボーナス（残り10秒超で+2、4秒超で+1）
- 信頼 = 回答品質に応じて増減（0〜100）／緊張 = 品質が低いほど上昇（0〜100）
- ランク: S(105+) / A(82+) / B(58+) / C

### 視点2種
| 視点 | 質問者 | 問われること |
|---|---|---|
| 発表者 | 営業部長・財務担当・支社長・同僚 | 結論先行、数字の根拠、責任と期限、反対意見への受け止め、決定依頼 |
| 質問される側 | 営業部長・監査担当ほか | 事実→原因→対策、確認範囲の明示、即答力 |

## 6. 未確定事項

- iPhone 実機での発熱とフレームレート（未検証）
- 音声認識の実機精度（iOS Safari は Web Speech API の対応が限定的）
- `document.modelContext` 経由の WebMCP ツール登録は実装済みだが、対応環境でのみ動作（未検証）
